import React from 'react'

import './spinner.css';

const Spinner = () => {
    return (
        <div className="spinner">
            <div className="circleloader"/>
        </div>
    );
};

export default Spinner;
